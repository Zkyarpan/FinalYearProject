// Create a new file src/types/session.ts
export enum SlotStatus {
    BOOKED = 'booked',
    IN_PROGRESS = 'inProgress',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
    MISSED = 'missed'
  }